# Leave empty to call the same origin — correct for Vercel (where /api is a
# serverless function) and for local dev (where Vite proxies /api).
# Set it only when the API lives on a different host.
VITE_API_BASE=

# Local dev only: where Vite proxies /api to.
VITE_DEV_API=http://127.0.0.1:8000
