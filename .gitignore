# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets — never commit a real .env
.env
.env.local
.env.*.local

# Node / frontend build
node_modules/
frontend/dist/
.vite/

# Vercel
.vercel/

# Scraped snapshots. These live on the `service-data` branch, which the
# workflows publish; a local scrape must never dirty the source branch.
/data/

# Static site build output
/site/
# `npm run data` drops a local copy of the published API here so the dev
# server serves exactly what Pages will serve. Generated, never committed.
/frontend/public/api/
/frontend/public/health.json
/frontend/public/ready.json

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
