# The scheduled scrapers' dependency set: fetch and parse, nothing else.
#
# The full requirements.txt additionally carries FastAPI, Starlette and uvicorn,
# which a batch job never imports. Dropping them takes the install from nine
# packages to five and the job's resident footprint down with it. Versions are
# pinned to exactly what requirements.txt pins, so the two deployments run
# identical code.
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.3
orjson==3.12.0
# MUFAP only: negotiates a standard browser TLS profile, which its Cloudflare
# bot management requires even though robots.txt permits the request.
curl_cffi==0.16.3
